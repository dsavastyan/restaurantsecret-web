// Partner self-service is archived until restaurants start maintaining their own menus.
// Keep the implementation in the repository so the flow can be restored later.
export const PARTNER_ADMIN_ENABLED = false
